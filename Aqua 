-- =============================================================================
-- AQUA PREMIUM - WINDOW OLUŞTURMA MOTORU (SOL PANEL EKSİKSİZ TAM SÜRÜM)
-- =============================================================================

local Menu = {}
Menu.__index = Menu

-- Gerekli Servisler
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

-- Tema Deposu (Gelişmiş Renk Paletleri)
local Themes = {
	Aqua = {
		MainBg = Color3.fromRGB(15, 20, 28),
		HeaderBg = Color3.fromRGB(22, 31, 43),
		ElementBg = Color3.fromRGB(28, 39, 55),
		Accent = Color3.fromRGB(0, 190, 255),
		TextMain = Color3.fromRGB(255, 255, 255),
		TextSub = Color3.fromRGB(160, 175, 190)
	},
	Inferno = {
		MainBg = Color3.fromRGB(20, 10, 10),
		HeaderBg = Color3.fromRGB(32, 15, 15),
		ElementBg = Color3.fromRGB(45, 20, 20),
		Accent = Color3.fromRGB(255, 60, 60),
		TextMain = Color3.fromRGB(255, 255, 255),
		TextSub = Color3.fromRGB(190, 140, 140)
	}
}

-- Ses Efektleri (SFX)
local SFX = {
	Click = "rbxassetid://8594181422",
	Close = "rbxassetid://6801456024"
}

-- YARDIMCI FONKSİYONLAR (UI Çizim ve Sürükleme Sistemleri)
local function playSound(assetId, volume)
	local s = Instance.new("Sound", game:GetService("CoreGui"))
	s.SoundId = assetId
	s.Volume = volume or 0.5
	s.PlayOnRemove = true
	s:Destroy()
end

local function addCorner(instance, radius)
	local c = Instance.new("UICorner")
	c.CornerRadius = UDim.new(0, radius)
	c.Parent = instance
	return c
end

local function addStroke(instance, color, thickness, transparency)
	local s = Instance.new("UIStroke")
	s.Color = color
	s.Thickness = thickness or 1
	s.Transparency = transparency or 0
	s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
	s.Parent = instance
	return s
end

local function makeDraggable(clickFrame, objectToMove)
	local dragging, dragInput, dragStart, startPos
	clickFrame.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = objectToMove.Position
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then dragging = false end
			end)
		end
	end)
	clickFrame.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)
	UserInputService.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			local delta = input.Position - dragStart
			TweenService:Create(objectToMove, TweenInfo.new(0.08, Enum.EasingStyle.Out), {
				Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
			}):Play()
		end
	end)
end

-- =============================================================================
-- ANA WINDOW OLUŞTURMA FONKSİYONU
-- =============================================================================
function Menu:CreateWindow(title, keyName, showButton, keySystemConfig)
	local self = setmetatable({}, Menu)

	-- Tema Kontrolü
	keySystemConfig = keySystemConfig or {}
	local selectedThemeName = keySystemConfig.MenuTheme or "Aqua"
	local activeTheme = Themes[selectedThemeName] or Themes.Aqua
	self.SelectedTheme = activeTheme

	-- Eski Arayüzleri Temizleme
	local existing = LocalPlayer:WaitForChild("PlayerGui"):FindFirstChild("AquaCSGO_PremiumMenu")
	if existing then existing:Destroy() end

	-- Ana Ekran Katmanı (ScreenGui)
	local ScreenGui = Instance.new("ScreenGui")
	ScreenGui.Name = "AquaCSGO_PremiumMenu"
	ScreenGui.ResetOnSpawn = false
	ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	ScreenGui.DisplayOrder = 9999
	ScreenGui.IgnoreGuiInset = true
	ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
	self.ScreenGui = ScreenGui

	-- ANA PANEL ÇERÇEVESİ (CanvasGroup ile Yumuşak Animasyonlar)
	local CanvasGroup = Instance.new("CanvasGroup")
	CanvasGroup.Name = "MainCanvas"
	CanvasGroup.Size = UDim2.new(0, 790, 0, 460)
	CanvasGroup.Position = UDim2.new(0.5, -395, 0.5, -230)
	CanvasGroup.BackgroundTransparency = 1
	CanvasGroup.GroupTransparency = 0
	CanvasGroup.Parent = ScreenGui
	self.CanvasGroup = CanvasGroup

	local MainFrame = Instance.new("Frame")
	MainFrame.Name = "MainFrame"
	MainFrame.Size = UDim2.new(1, 0, 1, 0)
	MainFrame.BackgroundColor3 = activeTheme.MainBg
	MainFrame.BackgroundTransparency = 0.15
	MainFrame.ClipsDescendants = true
	MainFrame.Parent = CanvasGroup
	self.MainFrame = MainFrame

	addCorner(MainFrame, 10)
	local FrameStroke = addStroke(MainFrame, activeTheme.Accent, 1.2, 0.3)

	-- ÜST BAŞLIK (HEADER BAR)
	local Header = Instance.new("Frame")
	Header.Size = UDim2.new(1, 0, 0, 40)
	Header.BackgroundColor3 = activeTheme.HeaderBg
	Header.BackgroundTransparency = 0.2
	Header.BorderSizePixel = 0
	Header.ZIndex = 10
	Header.Parent = MainFrame
	addCorner(Header, 10)

	local HeaderFix = Instance.new("Frame")
	HeaderFix.Size = UDim2.new(1, 0, 0, 10)
	HeaderFix.Position = UDim2.new(0, 0, 1, -10)
	HeaderFix.BackgroundColor3 = activeTheme.HeaderBg
	HeaderFix.BackgroundTransparency = 0.2
	HeaderFix.ZIndex = 10
	HeaderFix.Parent = Header

	local TitleLabel = Instance.new("TextLabel")
	TitleLabel.Size = UDim2.new(0, 300, 1, 0)
	TitleLabel.Position = UDim2.new(0, 18, 0, 0)
	TitleLabel.BackgroundTransparency = 1
	TitleLabel.Text = string.upper(title or "AQUA PREMIUM")
	TitleLabel.Font = Enum.Font.SourceSansBold
	TitleLabel.TextSize = 18
	TitleLabel.TextColor3 = activeTheme.TextMain
	TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
	TitleLabel.ZIndex = 10
	TitleLabel.Parent = Header

	-- KONTROL BUTONLARI (KÜÇÜLT / KAPAT)
	local Controls = Instance.new("Frame")
	Controls.Size = UDim2.new(0, 90, 1, 0)
	Controls.Position = UDim2.new(1, -95, 0, 0)
	Controls.BackgroundTransparency = 1
	Controls.ZIndex = 10
	Controls.Parent = Header

	local MinBtn = Instance.new("TextButton")
	MinBtn.Size = UDim2.new(0, 32, 0, 24)
	MinBtn.Position = UDim2.new(0, 5, 0.5, -12)
	MinBtn.BackgroundColor3 = activeTheme.ElementBg
	MinBtn.BackgroundTransparency = 0.4
	MinBtn.Text = "-"
	MinBtn.TextColor3 = activeTheme.TextMain
	MinBtn.Font = Enum.Font.SourceSansBold
	MinBtn.TextSize = 18
	MinBtn.AutoButtonColor = false
	MinBtn.ZIndex = 10
	MinBtn.Parent = Controls
	addCorner(MinBtn, 6)

	local CloseBtn = Instance.new("TextButton")
	CloseBtn.Size = UDim2.new(0, 32, 0, 24)
	CloseBtn.Position = UDim2.new(0, 42, 0.5, -12)
	CloseBtn.BackgroundColor3 = Color3.fromRGB(80, 30, 30)
	CloseBtn.BackgroundTransparency = 0.4
	CloseBtn.Text = "X"
	CloseBtn.TextColor3 = Color3.fromRGB(255, 120, 120)
	CloseBtn.Font = Enum.Font.SourceSansBold
	CloseBtn.TextSize = 15
	CloseBtn.AutoButtonColor = false
	CloseBtn.ZIndex = 10
	CloseBtn.Parent = Controls
	addCorner(CloseBtn, 6)

	-- İÇERİK ALANI (SAĞ TARAF)
	local Container = Instance.new("Frame")
	Container.Size = UDim2.new(1, -314, 1, -52)
	Container.Position = UDim2.new(0, 302, 0, 48)
	Container.BackgroundTransparency = 1
	Container.ClipsDescendants = true
	Container.Parent = MainFrame
	self.Container = Container

	-- SAĞ ALT RESIZE (BOYUTLANDIRMA) TUTAMACI
	local ResizeGrip = Instance.new("ImageButton")
	ResizeGrip.Size = UDim2.new(0, 16, 0, 16)
	ResizeGrip.Position = UDim2.new(1, -16, 1, -16)
	ResizeGrip.BackgroundTransparency = 1
	ResizeGrip.Image = "rbxassetid://3858618954"
	ResizeGrip.ImageColor3 = activeTheme.Accent
	ResizeGrip.ZIndex = 12
	ResizeGrip.Parent = MainFrame

	makeDraggable(Header, CanvasGroup)

	-- Dinamik Boyutlandırma Motoru
	local resizing, resizeStart, startSize
	ResizeGrip.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			resizing = true
			resizeStart = input.Position
			startSize = Vector2.new(CanvasGroup.Size.X.Offset, CanvasGroup.Size.Y.Offset)
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then resizing = false end
			end)
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if resizing and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			local delta = input.Position - resizeStart
			local newX = math.clamp(startSize.X + delta.X, 650, 1300)
			local newY = math.clamp(startSize.Y + delta.Y, 400, 900)
			TweenService:Create(CanvasGroup, TweenInfo.new(0.05, Enum.EasingStyle.Linear), {Size = UDim2.new(0, newX, 0, newY)}):Play()
		end
	end)

	-- =============================================================================
	-- 🛠 SİLİNEN ÖZELLİK: GERİ GETİRİLEN SOL DIAGNOSTICS & PROFiL PANELİ
	-- =============================================================================
	local DiagnosticFrame = Instance.new("Frame")
	DiagnosticFrame.Name = "Hardcoded_DiagnosticsPanel"
	DiagnosticFrame.Size = UDim2.new(0, 278, 1, -52)
	DiagnosticFrame.Position = UDim2.new(0, 12, 0, 48)
	DiagnosticFrame.BackgroundColor3 = activeTheme.HeaderBg
	DiagnosticFrame.BackgroundTransparency = 0.5
	DiagnosticFrame.Parent = MainFrame
	addCorner(DiagnosticFrame, 8)
	addStroke(DiagnosticFrame, activeTheme.Accent, 1, 0.6)

	-- Oyuncu Profil Resmi (Avatar)
	local AvatarImage = Instance.new("ImageLabel")
	AvatarImage.Size = UDim2.new(0, 46, 0, 46)
	AvatarImage.Position = UDim2.new(0, 14, 0, 14)
	AvatarImage.BackgroundColor3 = activeTheme.ElementBg
	AvatarImage.Parent = DiagnosticFrame
	addCorner(AvatarImage, 23)
	addStroke(AvatarImage, activeTheme.Accent, 1, 0.4)

	task.spawn(function()
		local thumbType = Enum.ThumbnailType.HeadShot
		local thumbSize = Enum.ThumbnailSize.Size150x150
		local content, isReady = Players:GetUserThumbnailAsync(LocalPlayer.UserId, thumbType, thumbSize)
		if isReady then AvatarImage.Image = content end
	end)

	-- Oyuncu Display Name & Kullanıcı Adı Bilgisi
	local DisplayNameLabel = Instance.new("TextLabel")
	DisplayNameLabel.Size = UDim2.new(1, -78, 0, 18)
	DisplayNameLabel.Position = UDim2.new(0, 72, 0, 16)
	DisplayNameLabel.BackgroundTransparency = 1
	DisplayNameLabel.Text = LocalPlayer.DisplayName
	DisplayNameLabel.Font = Enum.Font.SourceSansBold
	DisplayNameLabel.TextSize = 15
	DisplayNameLabel.TextColor3 = activeTheme.TextMain
	DisplayNameLabel.TextXAlignment = Enum.TextXAlignment.Left
	DisplayNameLabel.Parent = DiagnosticFrame

	local UsernameLabel = Instance.new("TextLabel")
	UsernameLabel.Size = UDim2.new(1, -78, 0, 14)
	UsernameLabel.Position = UDim2.new(0, 72, 0, 34)
	UsernameLabel.BackgroundTransparency = 1
	UsernameLabel.Text = "@" .. LocalPlayer.Name
	UsernameLabel.Font = Enum.Font.SourceSans
	UsernameLabel.TextSize = 13
	UsernameLabel.TextColor3 = activeTheme.TextSub
	UsernameLabel.TextXAlignment = Enum.TextXAlignment.Left
	UsernameLabel.Parent = DiagnosticFrame

	-- Performans ve Sunucu Durum Kutusu (FPS vb.)
	local ServerInfoFrame = Instance.new("Frame")
	ServerInfoFrame.Size = UDim2.new(1, -24, 1, -85)
	ServerInfoFrame.Position = UDim2.new(0, 12, 0, 72)
	ServerInfoFrame.BackgroundColor3 = activeTheme.MainBg
	ServerInfoFrame.BackgroundTransparency = 0.4
	ServerInfoFrame.Parent = DiagnosticFrame
	addCorner(ServerInfoFrame, 6)
	addStroke(ServerInfoFrame, activeTheme.ElementBg, 1, 0.5)

	local FpsLabel = Instance.new("TextLabel")
	FpsLabel.Size = UDim2.new(1, -12, 0, 25)
	FpsLabel.Position = UDim2.new(0, 10, 0, 10)
	FpsLabel.BackgroundTransparency = 1
	FpsLabel.Text = "Sistem Durumu: Hesaplanıyor..."
	FpsLabel.Font = Enum.Font.SourceSansBold
	FpsLabel.TextSize = 14
	FpsLabel.TextColor3 = Color3.fromRGB(0, 255, 140)
	FpsLabel.TextXAlignment = Enum.TextXAlignment.Left
	FpsLabel.Parent = ServerInfoFrame

	-- Gerçek Zamanlı FPS Hesaplama Motoru
	local lastIteration, frameCount = os.clock(), 0
	RunService.RenderStepped:Connect(function()
		frameCount = frameCount + 1
		local currentTime = os.clock()
		if currentTime - lastIteration >= 1 then
			FpsLabel.Text = "FPS: " .. tostring(frameCount) .. " | Stabil"
			frameCount = 0
			lastIteration = currentTime
		end
	end)

	-- =============================================================================
	-- ETKİLEŞİMLER VE KAPATMA BUTON FONKSİYONLARI
	-- =============================================================================
	CloseBtn.MouseButton1Click:Connect(function()
		playSound(SFX.Close, 0.6)
		local tw = TweenService:Create(CanvasGroup, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(0, 790, 0, 0), GroupTransparency = 1})
		tw:Play()
		tw.Completed:Connect(function() ScreenGui:Destroy() end)
	end)

	local minimized = false
	MinBtn.MouseButton1Click:Connect(function()
		playSound(SFX.Click, 0.5)
		minimized = not minimized
		local targetSize = minimized and UDim2.new(0, 790, 0, 40) or UDim2.new(0, 790, 0, 460)
		TweenService:Create(CanvasGroup, TweenInfo.new(0.35, Enum.EasingStyle.Exponential, Enum.EasingDirection.Out), {Size = targetSize}):Play()
		Container.Visible = not minimized
		DiagnosticFrame.Visible = not minimized
	end)

	self.MenuVisible = true
	return self
end

return Menu
